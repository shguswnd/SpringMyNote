최상단 BeanFactory가 있고 이걸 상속받는
ApplicationContext가 있다.


컨 + n으로 검색하며 왼우측상단 all을 눌러 들어가서 상속된걸 보면 빈팩토리가 나온다.

ApplicationContext 를 보면 여러개의 부가기능이있다.


xml 만들어보자

test 코드에 xml 패키지 만들기, -> XmlAppContext 만든다.

xml인 자바 코드가 아닌것은 resouce에다가 다 만들면 된다.
appConfig.xml