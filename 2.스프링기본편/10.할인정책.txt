DIP, OCP를 못지키는 문제점 


이전 쿠폰제에서 점율로 퍼센티지 기능으로 바꾼다고 치고 코드를 만들것이다.


discount 패키지에서 RateDiscountPolicy 클래스를 만든다.


JUnit은 DisplayName 로 한글로 이름을 쓸수 있다.


테스트 코드는 가장 중요한게 성공을 만들고 실패도 반드시 만들어주는게 좋다

이렇게해야 코드가 더 쓸께 없어짐


Assertions 는 static으로 해주는게 좋다 알트 + 엔터

Tip
만든 파일에 테스트 클래스를 자동으로 만들어 주려면

컨 + 시 + T 를 눌려주면 되고 testing library가 JUnit5인지만 판단하고 만들면 된다.

