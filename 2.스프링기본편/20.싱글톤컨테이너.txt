test 패키지 안에 
Singleton 패키지를 만들고
SingletonTest 클래스 생성

테스트를 보면 객체를 생성할때 마다 주소값이 다르다.

DI 컨테이너인 AppConfig는 요청을 할 때 마다 객체를 새로  생성한다.
고객 트래픽이 초당 100이 나오면 초당 100개 객체가 생성되고 소멸된다.
해당 방안은 객체가 딱 1개만 생성되고, 공유하도록 설계하면 된다.
--싱글톤 패턴


