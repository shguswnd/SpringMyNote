다대다는 실무에서 안쓴다.

보통 가장 많이쓰는게 다대일 그 다름 일대다이다.

일대일은 가끔 나옴.

테이블은 외래키 하나로 조인하기 때문에 방향이라는 개념이 없다.

하지만 객체는 참조용 필드가 있어야 있는쪽으로 참조가 가능핟.


객체 양방향 관계는 참조가 2군데 있고 외래키 관리를 연관관계 주인으로 만든다.

주인의 반대편은 외래키에 영향을 주지 않고 단순 조회만 가능


항상 일대다일경우 다에 외래키가 가야한다.

일대다 단방향 모델은 거의 사용하지 않는다.
팀 <-> 멤버에서 기존 멤버에 fk를 두었다면 지금은
팀에 멤버의 list값을 두고 생각을 해보자.

근데 DB 설계상 fk를 다쪽에 주지 일쪽에 주나?
당연히 안된다.

Jpa서 @JoinColumn(insertable=false, updatable=false)
를 두어 읽기 전용 필드를 사용해서 양방향 처럼 사용하는 방법이 있다.
