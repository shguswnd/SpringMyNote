서블릿은 비지니스 로직을 제외한 모든 과정을 전부 지원해준다.

서블릿 컨테이너 : 
서블릿 객체 소스 만들걸 개발자는 코드만 만든다.
그러면 was의 서블릿 컨테이너는 이 서블릿 객체를 생성 및 호출해준다.
was가 죽으면 서브릿도 죽여주는 생명주기를 돌려준다.

**서블릿 객체는 싱글톤으로 관리된다.
**공유변수(멤버변수) 사용할때 주의
**동시 요청을 위한 멀티 쓰레드 처리 지원(was가 다 처리해준다.)
